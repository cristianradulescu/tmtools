{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h3>Job title creation</h3>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary btn-sm" role="button" />
    {{ form_end(form) }}

    <a href="{{ path('jobtitle_index') }}" class="btn btn-primary btn-sm" role="button">Back to the list</a>

{% endblock %}
