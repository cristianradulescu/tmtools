{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h3>Edit "{{ jobTitle }}"</h3>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-primary btn-sm" role="button" />
    {{ form_end(edit_form) }}

    <a href="{{ path('jobtitle_index') }}" class="btn btn-primary btn-sm" role="button">Back to the list</a>
    {{ form_start(delete_form) }}
        <input type="submit" value="Delete" class="btn btn-danger btn-sm" role="button">
    {{ form_end(delete_form) }}

{% endblock %}
